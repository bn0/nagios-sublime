{ "name": "Nagios",
  "scopeName": "source.nagios",
  "fileTypes": ["cfg"],
  "patterns": [
    { "name": "comment.line.number-sign.nagios",
      "match": "^[\\s]*#.*",
      "comment": "# comments"
    },
    { "include": "#variable" },
    { "name": "entity.section.nagios",
      "begin": "^[\\s]*(define)[\\s]+([A-Za-z0-9_]+)[\\s]*(\\{)",
      "beginCaptures": {
        "1": { "name": "keyword.nagios" },
        "2": { "name": "constant.language.nagios" }
      },
      "patterns": [
        { "include": "$self" },
        { "include": "#variable" },
        { "include": "#separator" },
        { "name": "constant.language.nagios",
          "match": "^[\\s]*[A-Za-z0-9_]+[\\s]"
        },
        { "name": "comment.line.semi-colon.nagios",
          "match": ";.*",
          "comment": "In-line comments"
        }
      ],
      "end": "\\}"
    },
    { "name": "string.quoted.double.nagios",
      "begin": "\"",
      "beginCaptures": {
        "0": { "name": "string.quoted.double.nagios" }
      },
      "patterns": [
        { "include": "#variable" },
        { "include": "#separator" }
      ],
      "end": "\""
    }
  ],
  "repository": {
    "variable": {
      "name": "constant.other.nagios",
      "match": "\\$([A-Za-z][A-Za-z0-9_]+)\\$",
      "comment": "Variables like $SERVICESTATE$, $USER1$ etc"
    },
    "separator": {
      "name": "constant.character.nagios",
      "match": "!",
      "comment": "check_command separators"
    }
  },
  "uuid": "a8870a27-4f36-4aa7-8f7b-34a87060247d"
}